# Pipedream Policy Brief — Environment Variables

# ─── Supabase (daily_snapshot table, free tier) ───
SUPABASE_URL=https://ciuklhiswctbnffqvlhs.supabase.co
SUPABASE_ANON_KEY=sb_publishable_XXXX

# ─── HuggingFace Inference API (free tier) ───
# Used by daily narrative synthesis. Retrieve via:
#   security find-generic-password -s "huggingface-token" -w
HUGGINGFACE_API_KEY=hf_xxx

# ─── Vercel Cron ───
# Auto-set by Vercel in production. For local testing only.
CRON_SECRET=dev-only-secret

# ─── Potential future integrations (currently unused) ───
# GOOGLE_MAPS_API_KEY=       # Real-time station status
# GLOBALPETROLPRICES_KEY=    # Paid weekly benchmarks
